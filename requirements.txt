# --- FastAPI & ASGI Server ---
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
starlette>=0.36.0
websockets>=12.0

# --- Networking, Async & HTTP ---
httpx>=0.27.0
aiohttp>=3.9.0
requests>=2.31.0
python-dotenv>=1.0.0

# --- Worker LLM Client (OpenAI spec pod Ollamę z RTX 3090) ---
openai>=1.14.0

# --- Voice Engine (Lekki Piper TTS na CPU) ---
piper-tts>=1.2.0
soundfile>=0.12.1
numpy>=1.26.0

# --- Lekki PyTorch (Tylko CPU, bez CUDA - pod stare importy w http.py) ---
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0
torchaudio>=2.2.0

# --- Web UI & System Helpers ---
jinja2>=3.1.3
psutil>=5.9.8
rich>=13.7.0