# =============================================================================
# DAEMON Core — Server Dependencies (LXC Debian 12, CPU-only)
# =============================================================================
# ZAKAZ: Brak torch+CUDA, brak Coqui TTS, brak faster-whisper, brak openWakeWord.
# Te biblioteki naleza WYLACZNIE do client_node_win/requirements_client.txt.
# =============================================================================

# --- FastAPI & ASGI Server ---
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
starlette>=0.36.0
websockets>=12.0

# --- Networking, Async & HTTP ---
httpx>=0.27.0
python-dotenv>=1.0.0

# --- LLM Client (OpenAI-compat spec → Ollama na RTX 3090) ---
openai>=1.14.0

# --- Voice Engine: Piper TTS ONNX (CPU-only, brak CUDA) ---
piper-tts>=1.2.0
onnxruntime>=1.17.0
numpy>=1.26.0
soundfile>=0.12.1

# --- Web UI & System Helpers ---
jinja2>=3.1.3
psutil>=5.9.8
rich>=13.7.0neo4j>=5.14.0
